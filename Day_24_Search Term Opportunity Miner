function main(){

var report = AdsApp.report(
"SELECT Query, Clicks, Ctr FROM SEARCH_QUERY_PERFORMANCE_REPORT WHERE Clicks > 20 DURING LAST_7_DAYS"
);

var rows = report.rows();

while(rows.hasNext()){

var row = rows.next();

var ctr = parseFloat(row["Ctr"].replace("%",""));

if(ctr > 10){

Logger.log("Opportunity Keyword: "+row["Query"]);

}

}

}
