function main(){

var report = AdsApp.report(
"SELECT Query, Conversions FROM SEARCH_QUERY_PERFORMANCE_REPORT WHERE Conversions > 2 DURING LAST_30_DAYS"
);

var rows = report.rows();

while(rows.hasNext()){

var row = rows.next();

Logger.log("Add Exact Keyword: ["+row["Query"]+"]");

}

}
